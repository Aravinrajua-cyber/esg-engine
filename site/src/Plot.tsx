// Plotly has been removed from the runtime. All charts now use dependency-neutral inline React SVG
// (see ModelPerformance.tsx). This stub remains only so any stale import resolves to a no-op.
export default function Plot(): null {
  return null;
}
